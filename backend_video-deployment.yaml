apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend_video
  labels:
    run: backend_video
spec:
  replicas: 2
  selector:
    matchLabels:
      run: backend_video
  template:
    metadata:
      labels:
        run: backend_video
    spec:
      containers:
      - name: backend_video
        image: abdel124/udatube-api-video
        resources:
          limits:
            cpu: 500m
          requests:
            cpu: 200m
        env:
          - name: AWS_REGION
            valueFrom:
              configMapKeyRef:
                name:  env-configmap
                key: AWS_REGION         
          - name: JWT_SECRET
            valueFrom:
              configMapKeyRef:
                name:  env-configmap
                key: JWT_SECRET 
          - name: AWS_PROFILE
            valueFrom:
              configMapKeyRef:
                name:  env-configmap
                key: AWS_PROFILE 
          - name: AWS_BUCKET
            valueFrom:
              configMapKeyRef:
                name:  env-configmap
                key: AWS_BUCKET 
        ports:
        - containerPort: 80
        volumeMounts:
          - name: aws-secret
            mountPath: '/root/.aws'
            readOnly: true
      volumes:
        - name: aws-secret
          secret:
            secretName: aws-secret